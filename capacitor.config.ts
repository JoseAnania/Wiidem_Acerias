import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wiidem.acerias',
  appName: 'WiidemAcerias',
  webDir: 'out',
  server: {
    androidScheme: 'http'
  }
};

export default config;
